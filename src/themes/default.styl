/* theme: default */
.n-default {
    .msg-wrap {
       .n-icon {
            background-image: url(images/validator_default.png);
        } 
    }
    .n-right .msg-wrap {
        margin-top: 6px;
    }
    .n-tip {
        .n-icon {display:none;}
    }
}